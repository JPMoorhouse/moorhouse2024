---
const {
    bgColor,
    title,
    buttonText = "Get a Free Estimate",
    buttonSlug = "/",
} = Astro.props;
---

<div
    class={` ${bgColor}  rounded-[29px] gap-4 py-12 flex justify-between md:flex-row flex-col items-center sm:px-12 px-6 w-full`}
>
    <p class="lg:text-3xl text-2xl text-white font-medium">
        {title}
    </p>

    <a
        href={buttonSlug}
        class="rounded-full px-6 py-3 whitespace-nowrap font-medium md:w-fit w-full gap-4 bg-green text-white flex justify-center items-center"
    >
        {buttonText}
    </a>
</div>
